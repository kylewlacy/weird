{
  "name": "weird",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "format": "prettier . --write",
    "check-format": "prettier . --check"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
