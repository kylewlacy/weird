[package]
name = "weird-test-support"
version = "0.1.0"
license = "MIT"
edition = "2024"

[dependencies]
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
weird-core = { path = "../weird-core" }
